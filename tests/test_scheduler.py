import unittest
from core.scheduler import Scheduler
from core.player import Player

class TestScheduler(unittest.TestCase):
    def test_next_turn():
        player1 = Player("Joaco", )
       scheduler = Scheduler() 
